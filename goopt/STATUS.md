# GoOpt — Go Compiler Optimization Status

> ⚙️ Auto-generated by GoOpt. Last updated: 2026-04-30 12:55 UTC

## Overview

GoOpt iteratively modifies the Go compiler to improve code generation for
etcd's hot paths, validated by the project's benchmark suite.

## Current State

| Item | Value |
|------|-------|
| Go version | 1.26 (go.mod), system 1.26.2 |
| Side compiler | built (go1.26.0, requires GOTOOLCHAIN=local) |
| Compiler modifications | 0 active (1 attempted, reverted) |
| Profiling done | yes — 2026-04-30 |
| Optimization backlog | 3 items |
| Validated improvements | 0 |

## Hot Path Profile

From CPU profiling of `BenchmarkIndex*` (77.22s total samples):

| Function | Flat % | Cum % | Package |
|----------|--------|-------|---------|
| `(*keyIndex).compact` | 13.42% | 45.47% | mvcc |
| `(*generation).walk` | 13.12% | 23.25% | mvcc |
| `cmpbody` | 12.29% | 12.29% | runtime |
| `(*keyIndex).Less` | 7.94% | 20.46% | mvcc |
| `(*keyIndex).isEmpty` | 7.69% | 7.87% | mvcc |
| `items.find` | 1.04% | 25.67% | btree |
| `(*node).get` | 1.42% | 23.10% | btree |
| `sort.Search` | 1.11% | 23.79% | sort |
| `(*BTree).Get` | 0.01% | 23.12% | btree |
| `(*keyIndex).doCompact` | 0.36% | 23.61% | mvcc |

## Optimization Backlog

| Priority | Name | Description | Target | Expected Impact | Status |
|----------|------|-------------|--------|-----------------|--------|
| 1 | bce-key-index-hot | 21 bounds checks in key_index.go; `generation.walk` reverse-iteration `g.revs[l-i-1]` not proved safe | `ssa/prove.go` | 1-3% compact | untried |
| 2 | escape-keyindex-put | `keyIndex.put` append patterns escape to heap; GC ~9% CPU | `escape/escape.go` | 1-2% Put | untried |
| 3 | inline-btree-find-specialize | `items.find` (cost 119) and `node.get` (cost 167) too complex for inline budget; consider partial inlining of sort.Search pattern | `inline/inl.go` | 2-5% Get/Compact | untried |

## Completed Optimizations

No optimizations validated yet.

## Failed Attempts

| Date | Name | Description | Result | Lesson |
|------|------|-------------|--------|--------|
| 2026-04-30 | raise-inline-budget-85 | Raised `inlineMaxBudget` from 80 to 85 | No significant improvement | `BTree.Get` (cost 81) became inlinable but truly hot inner functions (`items.find` cost 119, `node.get` cost 167) remain too large. Only IndexCompact100 showed -1.28% (p=0.038), geomean -0.09%. Budget increase alone insufficient—need targeted BCE or escape analysis improvements. |

## Recent Activity

### 2026-04-30 12:41 UTC — Run (workflow 25165810568)
- **Tasks selected**: 1 (Setup Side Compiler), 8 (Monthly Status)
- **Tasks executed**: 1 (rebuilt side compiler), 4 (inline budget 80→85), 5 (benchmarked), 8, 9
- **Key findings**: Discovered `GOTOOLCHAIN=local` required to prevent toolchain directive override. Inline budget increase to 85 does not meaningfully help because hot BTree functions have costs 119-167, far exceeding the new budget. Reverted change.
- **Next steps**: Try bounds check elimination in `prove.go` for `generation.walk` reverse iteration pattern.

### 2026-04-30 12:06 UTC — Run (previous)
- **Tasks executed**: 1 (side compiler setup), 2 (profiling), 3 (codegen analysis), 8, 9
- **Key findings**: Identified hot paths and built optimization backlog. Top targets: BCE for key_index.go, escape analysis for Put path, partial inlining for btree functions.
